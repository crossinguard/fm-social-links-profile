---
import Social from './Social.astro';
import Challenge from './challenge/Challenge.astro';

interface Author {
  name: string;
}

import site from '../data/site.json';
const { author }: { author: Author } = site;
const today = new Date();
---

<footer>
  <Challenge />
  <div class="flex">
    <p>{author.name} &copy; {today.getFullYear()}</p>
    <p class="curious">stay curious</p>
    <Social />
  </div>
</footer>
<style>
  footer {
    border-block-start: solid var(--grey);
    margin-block-start: 2em;
  }
  .flex {
    font-family: monospace;
    display: flex;
    flex-direction: column;
    margin-block: 3em;
    color: var(--primary);
    text-align: center;
  }

  .curious {
    font-size: smaller;
    font-style: italic;
  }
</style>
